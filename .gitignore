# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
/dist/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Release signing — never commit keystores or their passwords
android/key.properties
*.jks
*.keystore

# AI model files — distributed separately via USB/local server, never committed
*.gguf
*.bin
*.ggml
*.litertlm
*.task
data/models/

# Workstation AutoAWQ output (tools/quantize_awq.py). Convert onward to
# GGUF / LiteRT; do not load these folders in the Flutter app.
assets/models/translation_4bit/
assets/models/brain_4bit/

# Build-time model tooling: llama.cpp checkout, release binaries, converter
# venv, and the intermediate f16 GGUF (quantize_translate_model.ps1)
/tools/.cache/
packages/ai-core/models/

# Flutter-generated files
.flutter-plugins
flutter_export_environment.sh

android/build/
windows/third_party/vulkan/
